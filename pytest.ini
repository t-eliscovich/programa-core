[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra --strict-markers --tb=short
filterwarnings =
    ignore::DeprecationWarning:flask_limiter.*
    ignore::DeprecationWarning:werkzeug.*
markers =
    db: tests that hit a real Postgres (skip by default; run with `pytest -m db`)
