Flask==3.0.3
psycopg2-binary>=2.9.10
bcrypt==4.1.3
python-dotenv==1.0.1
waitress==3.0.0
Jinja2==3.1.4
WTForms==3.1.2
Flask-WTF==1.2.1
Flask-Limiter==3.8.0
pyotp==2.9.0
qrcode[pil]==7.4.2
requests>=2.32.0  # cliente SOAP para SRI (modules/sri/envio.py) + Metabase adapter
Authlib==1.3.0  # Google OAuth login (modules/auth_google/)
openpyxl>=3.1  # Excel export — comisiones, bancos/movimientos. TMT 2026-05-20.
# Pillow recorta el blanco que sobra en el estado de cuenta sacado como IMAGEN
# (modules/_lib/imagen_motor.py). Ya venía instalado de arrastre por
# `qrcode[pil]`, pero desde el 25/08 lo usa código propio: se declara, así el
# día que el 2FA cambie de librería la foto de WhatsApp no se cae con él.
Pillow>=10.0
# dev / test
pytest==8.2.2
pytest-cov==5.0.0
# TMT 2026-08-13 (dueña): *"tarda mucho el deploy cada vez que hago cambios"*.
# Medido sobre los últimos 30 runs: de los 5m35s de push→producción, 3m57s
# eran los 4.303 tests `not db` corriendo en UN SOLO proceso mientras el
# runner tenía sus otros cores al pedo. xdist los reparte; el .coveragerc ya
# venía con parallel=True, así que el gate de 100% no se toca.
pytest-xdist==3.6.1
coverage[toml]==7.6.1
ruff==0.6.3
# pytest-postgresql permite correr tests marcados @pytest.mark.db contra una DB
# real. Requiere psycopg v3 — coexiste con psycopg2-binary que sigue siendo el
# driver del app en runtime.
pytest-postgresql>=6.1
psycopg[binary]>=3.2.10
dbfread>=2.0   # importador DBF → Postgres (proceso de transición; ver docs/RUNBOOK_sync_dbf.md)
boto3>=1.34
pypdf>=4.2  # merge de PDFs -- paquete de cierre de mes (modules/informes/cierres_paquete.py). TMT 2026-08-31.
