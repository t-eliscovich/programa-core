# Copy this file to .env and fill in real values. Never commit .env.

# Flask
SECRET_KEY=cambiame-por-una-cadena-larga-y-aleatoria
FLASK_ENV=development
FLASK_DEBUG=1

# PostgreSQL (LOCAL dev — restaurado desde intela12042026.sql)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=intela
DB_USER=postgres
DB_PASSWORD=postgres

# Pool sizing
DB_POOL_MIN=1
DB_POOL_MAX=10
