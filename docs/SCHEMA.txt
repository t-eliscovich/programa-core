=== public.catalogo ===
  producto character varying,
  categoria character varying

=== scintela.activos ===
  id_activos integer NOT NULL,
  fecha date,
  concepto character varying(100),
  tipo character varying(3),
  inicial numeric(9,2),
  amortizac numeric(9,2),
  amortimes numeric(9,2),
  valor numeric(9,2),
  cuota numeric(9,2),
  vida_util integer,
  id_proveedor integer,
  ult_mes_amortizado integer,
  fecha_crea timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  fecha_modifica timestamp without time zone,
  usuario_crea character varying(50),
  usuario_modifica character varying(50)

=== scintela.ahistoria ===
  id_historia integer NOT NULL,
  fecha date,
  stock numeric(12,2),
  kcom numeric(12,2),
  ktej numeric(12,2),
  ktin numeric(12,2),
  ustock numeric(12,2),
  uqui numeric(12,2),
  kvent numeric(12,2),
  uvent numeric(12,2),
  costo numeric(12,2),
  ucom numeric(12,2),
  utej numeric(12,2),
  utin numeric(12,2),
  gasto numeric(12,2),
  gstotal numeric(21,2),
  banco numeric(12,2),
  cart numeric(12,2),
  deuda numeric(12,2),
  retiro numeric(12,2),
  patrimonio numeric(12,2),
  anticipos numeric(12,2),
  dolar numeric(12,2),
  maquinaria numeric(21,2),
  realty numeric(12,2),
  usret numeric(21,2),
  usuti numeric(12,2),
  fecha_crea timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  fecha_modifica timestamp without time zone,
  usuario_crea character varying(50),
  usuario_modifica character varying(50)

=== scintela.banco ===
  no_banco integer NOT NULL,
  nombre character varying(50),
  fecha_crea timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  fecha_modifica timestamp without time zone,
  usuario_crea character varying(50),
  usuario_modifica character varying(50)

=== scintela.bitacora_migracion ===
  id integer NOT NULL,
  fecha_hora timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  nivel character varying(20) NOT NULL,
  mensaje text NOT NULL,
  usuario_pc character varying(100)

=== scintela.caja ===
  id_caja integer NOT NULL,
  fecha date,
  tipo character varying(3),
  importe numeric(9,2),
  concepto character varying(100),
  saldo numeric(9,2),
  clave character varying(3),
  id_cheque integer,
  fecha_crea timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  fecha_modifica timestamp without time zone,
  usuario_crea character varying(50),
  usuario_modifica character varying(50)

=== scintela.capital ===
  id_capital integer NOT NULL,
  fecha date,
  doc character varying(5),
  concepto character varying(50),
  importe numeric(21,2),
  invanual numeric(21,2),
  capital numeric(21,2),
  util numeric(21,2),
  patri numeric(21,2),
  clave character varying(3),
  fecha_crea timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  fecha_modifica timestamp without time zone,
  usuario_crea character varying(50),
  usuario_modifica character varying(50)

=== scintela.cheque ===
  id_cheque integer NOT NULL,
  no_cheque character varying(10),
  fecha date NOT NULL,
  fechad date NOT NULL,
  codigo_cli character varying(5),
  importe numeric(9,2),
  no_banco integer,
  banco character varying(30),
  stat character varying(5),
  fechaing date,
  fechaout date,
  prov character varying(5),
  clave character varying(5),
  numero_transaccion integer,
  id_cheque_padre integer,
  pasaconta integer,
  fecha_crea timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  fecha_modifica timestamp without time zone,
  usuario_crea character varying(50),
  usuario_modifica character varying(50)

=== scintela.chequesxfact ===
  id_chequexfact integer NOT NULL,
  id_cheque integer,
  id_fact integer,
  fechaing date NOT NULL,
  codigo_cli character varying(5),
  importe numeric(9,2),
  no_banco integer,
  tipo character varying(5),
  stat_f character varying(5),
  fecha_venci_f date,
  abono_f numeric(9,2),
  saldo_f numeric(9,2),
  fecha_crea timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  fecha_modifica timestamp without time zone,
  usuario_crea character varying(50),
  usuario_modifica character varying(50)

=== scintela.chequextransaccion ===
  id_chequextransacc integer NOT NULL,
  id_cheque integer,
  id_transaccion integer,
  fecha date NOT NULL,
  stat_ch character varying(3),
  fecha_crea timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  fecha_modifica timestamp without time zone,
  usuario_crea character varying(50),
  usuario_modifica character varying(50)

=== scintela.cliente ===
  id_cliente integer NOT NULL,
  codigo_cli character varying(3),
  nombre character varying(200),
  telefono character varying(30),
  ruc character varying(16),
  correo character varying(50),
  direccion1 character varying(200),
  direccion2 character varying(200),
  stop character varying(1),
  cupo integer,
  fecha_cupo date,
  clave character varying(3),
  pase character varying(2),
  id_ubicacion integer,
  no_banco integer,
  descuento numeric(9,2),
  pago character varying(2),
  observacion character varying(200),
  vend character varying(50),
  provincia character varying(50),
  canton character varying(50),
  parroquia character varying(50),
  cliente character varying(50),
  fecha_crea timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  fecha_modifica timestamp without time zone,
  usuario_crea character varying(50),
  usuario_modifica character varying(50)

=== scintela.cobro ===
  id_cobro integer NOT NULL,
  codigo_cli character varying(5),
  valor numeric(9,2),
  fecha date NOT NULL,
  banco character varying(20),
  tipo_doc character varying(20),
  no_docpago integer,
  id_factura integer,
  abono_total character varying(10),
  fecha_crea timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  fecha_modifica timestamp without time zone,
  usuario_crea character varying(50),
  usuario_modifica character varying(50)

=== scintela.compra ===
  id_compra integer NOT NULL,
  fecha date,
  id_proveedor integer,
  codigo_prov character varying(3),
  tipo character varying(3),
  comprobante character varying(100),
  kg numeric(9,2),
  importe numeric(9,2),
  numero integer,
  fecha_ing date,
  fechad date,
  concepto character varying(200),
  clave character varying(3),
  no_banco integer,
  fecha_crea timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  fecha_modifica timestamp without time zone,
  usuario_crea character varying(50),
  usuario_modifica character varying(50)

=== scintela.costos ===
  id_costo integer NOT NULL,
  cod character varying(5),
  color character varying(50),
  costo numeric(9,2),
  clase integer,
  fecha_crea timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  fecha_modifica timestamp without time zone,
  usuario_crea character varying(50),
  usuario_modifica character varying(50)

=== scintela.cuenta_bancaria ===
  no_cuenta character varying(50) NOT NULL,
  no_banco integer NOT NULL,
  banco character varying(50),
  tipo_cuenta character varying(5),
  fecha_crea timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  fecha_modifica timestamp without time zone,
  usuario_crea character varying(50),
  usuario_modifica character varying(50)

=== scintela.dolares ===
  id_dolares integer NOT NULL,
  fecha date,
  cta character varying(3),
  concepto character varying(100),
  importe numeric(9,2),
  st character varying(3),
  clave character varying(3),
  fecha_crea timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  fecha_modifica timestamp without time zone,
  usuario_crea character varying(50),
  usuario_modifica character varying(50)

=== scintela.estados ===
  id_estado integer NOT NULL,
  estado character varying(20),
  fecha_crea timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  fecha_modifica timestamp without time zone,
  usuario_crea character varying(50),
  usuario_modifica character varying(50)

=== scintela.factura ===
  id_factura integer NOT NULL,
  numf integer NOT NULL,
  fecha date NOT NULL,
  codigo_cli character varying(5) NOT NULL,
  kg numeric(9,2) DEFAULT 0 NOT NULL,
  importe numeric(9,2) DEFAULT 0 NOT NULL,
  abono numeric(9,2) DEFAULT 0,
  saldo numeric(9,2) DEFAULT 0,
  stat character varying(2),
  condic character varying(2),
  vencimiento date,
  tipo character varying(2),
  clave character varying(2),
  pase character varying(5),
  id_documento integer,
  numf_completo character varying(20),
  fecha_crea timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  fecha_modifica timestamp without time zone,
  usuario_crea character varying(50),
  usuario_modifica character varying(50)

=== scintela.facturas_cubiertas ===
  id integer NOT NULL,
  factura_cubierta_id integer,
  factura_fuente_id integer,
  monto numeric(12,2),
  fecha timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  monto_fuente numeric(12,2) DEFAULT 0,
  saldo_anterior numeric(12,2) DEFAULT 0,
  abono_anterior numeric(12,2) DEFAULT 0,
  abono_anterior_fuente numeric(12,2) DEFAULT 0,
  fecha_crea timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  fecha_modifica timestamp without time zone,
  usuario_crea character varying(50),
  usuario_modifica character varying(50)

=== scintela.flujo ===
  id_flujo integer NOT NULL,
  fecha date,
  cheques numeric(9,2),
  facturas numeric(9,2),
  posdat2 numeric(9,2),
  inter numeric(9,2),
  posdat1 numeric(9,2),
  pichincha numeric(9,2),
  mprima numeric(9,2),
  gastos numeric(9,2),
  saldo numeric(9,2),
  pagos numeric(9,2),
  dolares numeric(9,2),
  usaldo numeric(9,2),
  fecha_crea timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  fecha_modifica timestamp without time zone,
  usuario_crea character varying(50),
  usuario_modifica character varying(50)

=== scintela.grupo_cliente ===
  codigo_padre character varying(10) NOT NULL,
  codigo_hijo character varying(10) NOT NULL,
  fecha_crea timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  fecha_modifica timestamp without time zone,
  usuario_crea character varying(50),
  usuario_modifica character varying(50)

=== scintela.historia ===
  id_historia integer NOT NULL,
  fecha date,
  stock numeric(12,2),
  kcom numeric(12,2),
  ktej numeric(12,2),
  ktin numeric(12,2),
  ustock numeric(12,2),
  uqui numeric(12,2),
  kvent numeric(12,2),
  uvent numeric(12,2),
  costo numeric(12,2),
  ucom numeric(12,2),
  utej numeric(12,2),
  utin numeric(12,2),
  gasto numeric(12,2),
  gstotal numeric(21,2),
  banco numeric(12,2),
  cart numeric(12,2),
  deuda numeric(12,2),
  retiro numeric(12,2),
  patrimonio numeric(12,2),
  anticipos numeric(12,2),
  dolar numeric(12,2),
  maquinaria numeric(21,2),
  realty numeric(12,2),
  usret numeric(21,2),
  usuti numeric(12,2),
  fecha_crea timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  fecha_modifica timestamp without time zone,
  usuario_crea character varying(50),
  usuario_modifica character varying(50)

=== scintela.iniciales ===
  id_iniciales integer NOT NULL,
  mesnum integer,
  mesnom character varying(10),
  yy integer,
  hilado numeric(9,2),
  tejido numeric(9,2),
  terminado numeric(9,2),
  vq numeric(9,2),
  um numeric,
  uk numeric,
  uf numeric,
  uq numeric,
  pre numeric(9,2),
  kprog numeric(9,2),
  gprog numeric(9,2),
  numnot numeric(9,2),
  dificil numeric(9,2),
  pretej numeric(9,2),
  pretin numeric(9,2),
  preadm numeric(9,2),
  pretot numeric(9,2),
  fecha_crea timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  fecha_modifica timestamp without time zone,
  usuario_crea character varying(50),
  usuario_modifica character varying(50)

=== scintela.posdat ===
  id_posdat integer NOT NULL,
  fecha date,
  fechad date,
  prov character varying(3),
  num integer,
  importe numeric(9,2),
  concepto character varying(100),
  banc integer,
  clave character varying(3),
  fecha_crea timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  fecha_modifica timestamp without time zone,
  usuario_crea character varying(50),
  usuario_modifica character varying(50)

=== scintela.precios ===
  clase integer NOT NULL,
  descripcio character varying(50),
  jersey numeric(9,2),
  pique numeric(9,2),
  toper numeric(9,2),
  alemania numeric(9,2),
  rib numeric(9,2),
  cuellos numeric(9,2),
  lycra numeric(9,2),
  falso numeric(9,2),
  kiana numeric(9,2),
  medical numeric(9,2),
  micro numeric(9,2),
  james numeric(9,2),
  fecha_crea timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  fecha_modifica timestamp without time zone,
  usuario_crea character varying(50),
  usuario_modifica character varying(50)

=== scintela.producto ===
  id_producto integer NOT NULL,
  activo boolean DEFAULT true NOT NULL,
  codigo character varying(50) NOT NULL,
  descripcion character varying(500),
  nombre character varying(300) NOT NULL,
  nombre_comercial character varying(500) NOT NULL,
  precio_ultima_venta numeric(12,4) NOT NULL,
  tipo_producto integer NOT NULL,
  id_unidad integer NOT NULL,
  id_unidad_almacenamiento integer,
  id_unidad_compra integer,
  id_unidad_informativa integer,
  id_unidad_venta integer,
  codigo_categoria_producto character varying(15) NOT NULL,
  codigo_subcategoria_producto character varying(15) NOT NULL,
  id_categoria_producto integer NOT NULL,
  id_subcategoria_producto integer NOT NULL,
  nombre_categoria_producto character varying(255) NOT NULL,
  nombre_subcategoria_producto character varying(255) NOT NULL,
  nombre_nivel character varying(50),
  nombre_fiscal_proveedor_ultima_compra character varying(300),
  fecha_crea timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  fecha_modifica timestamp without time zone,
  usuario_crea character varying(50),
  usuario_modifica character varying(50)

=== scintela.proforma_cabecera ===
  id_proforma integer NOT NULL,
  id_cliente integer NOT NULL,
  fecha_emision date DEFAULT CURRENT_DATE,
  subtotal numeric(10,2),
  porcentaje_descuento_volumen numeric(5,2),
  monto_descuento_volumen numeric(10,2),
  subtotal_con_descuento numeric(10,2),
  aplica_descuento_contado boolean,
  monto_descuento_contado numeric(10,2),
  total_final numeric(10,2),
  observaciones text,
  usuario_creacion character varying(50),
  fecha_crea timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  fecha_modifica timestamp without time zone,
  usuario_crea character varying(50),
  usuario_modifica character varying(50)

=== scintela.proforma_detalle ===
  id_detalle integer NOT NULL,
  id_proforma integer,
  id_subcategoria_producto integer,
  nombre_producto character varying(100),
  color character varying(50),
  cantidad_kilos numeric(10,2),
  precio_unitario numeric(10,2),
  precio_total numeric(10,2),
  fecha_crea timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  fecha_modifica timestamp without time zone,
  usuario_crea character varying(50),
  usuario_modifica character varying(50)

=== scintela.proveedor ===
  id_proveedor integer NOT NULL,
  codigo_prov character varying(3),
  nombre character varying(200),
  telefono character varying(30),
  ruc character varying(16),
  correo character varying(50),
  direccion character varying(200),
  representante character varying(200),
  tipo character varying(1),
  plazo integer,
  retbase integer,
  retiva integer,
  codigo_imp integer,
  activo character varying(2),
  fecha_crea timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  fecha_modifica timestamp without time zone,
  usuario_crea character varying(50),
  usuario_modifica character varying(50)

=== scintela.provisiones ===
  id_provisiones integer NOT NULL,
  concepto character varying(50),
  importe numeric(9,2),
  periodo_aplica character varying(30),
  fecha_crea timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  fecha_actualiza timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  usuario_crea character varying(20),
  usuario_actualiza character varying(30)

=== scintela.retencion ===
  id_retencion integer NOT NULL,
  codigo_cli character varying(50),
  rete numeric(9,2),
  numf integer,
  fecha date DEFAULT CURRENT_TIMESTAMP,
  fecha_crea timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  fecha_modifica timestamp without time zone,
  usuario_crea character varying(50),
  usuario_modifica character varying(50)

=== scintela.retiros ===
  id_retiro integer NOT NULL,
  fecha date,
  nb integer,
  ret numeric(9,2),
  de character varying(5),
  concepto character varying(100),
  clave character varying(5),
  id_transaccion_bancaria integer,
  fecha_crea timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  fecha_modifica timestamp without time zone,
  usuario_crea character varying(50),
  usuario_modifica character varying(50)

=== scintela.subcategoria_producto ===
  codigo_categoria_producto character varying(15) NOT NULL,
  codigo_subcategoria_producto character varying(15) NOT NULL,
  id_categoria_producto integer NOT NULL,
  id_subcategoria_producto integer NOT NULL,
  nombre_categoria_producto character varying(255) NOT NULL,
  nombre_subcategoria_producto character varying(255) NOT NULL,
  fecha_crea timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  fecha_modifica timestamp without time zone,
  usuario_crea character varying(50),
  usuario_modifica character varying(50)

=== scintela.tinto ===
  id_tinto integer NOT NULL,
  fecha date,
  cod character varying(5),
  color character varying(30),
  franela integer,
  messi integer,
  james integer,
  jersey integer,
  j3 integer,
  toper integer,
  jlyc integer,
  pique integer,
  flyc integer,
  falso integer,
  otros integer,
  kiana integer,
  tipo integer,
  kg numeric(9,2),
  kgn numeric(9,2),
  importe numeric(9,2),
  stat character varying(3),
  clave character varying(3),
  fecha_crea timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  fecha_modifica timestamp without time zone,
  usuario_crea character varying(50),
  usuario_modifica character varying(50)

=== scintela.transacciones_bancarias ===
  id_transaccion integer NOT NULL,
  fecha date NOT NULL,
  documento character varying(5) NOT NULL,
  concepto character varying(50) NOT NULL,
  fechad date,
  importe numeric(9,2) DEFAULT 0 NOT NULL,
  saldo numeric(9,2) DEFAULT 0,
  stat character varying(2),
  no_banco integer,
  no_cta character varying(20),
  prov character varying(5),
  numreferencia integer,
  clave character varying(3),
  fecha_crea timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  fecha_modifica timestamp without time zone,
  usuario_crea character varying(50),
  usuario_modifica character varying(50)

=== scintela.xfactura ===
  id_factura integer NOT NULL,
  numf integer NOT NULL,
  fecha date NOT NULL,
  codigo_cli character varying(5) NOT NULL,
  kg numeric(9,2) DEFAULT 0 NOT NULL,
  importe numeric(9,2) DEFAULT 0 NOT NULL,
  abono numeric(9,2) DEFAULT 0,
  saldo numeric(9,2) DEFAULT 0,
  stat character varying(2),
  condic character varying(2),
  vencimiento date,
  tipo character varying(2),
  clave character varying(2),
  pase character varying(5),
  fecha_crea timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  fecha_modifica timestamp without time zone,
  usuario_crea character varying(50),
  usuario_modifica character varying(50)

=== scintela.xgast ===
  id_xgast integer NOT NULL,
  fecha date,
  doc character varying(5),
  prov character varying(5),
  concepto character varying(100),
  num integer,
  fechad date,
  importe numeric(9,2),
  saldo numeric(9,2),
  stat character varying(3),
  clave character varying(3),
  fecha_crea timestamp without time zone DEFAULT CURRENT_TIMESTAMP,
  fecha_modifica timestamp without time zone,
  usuario_crea character varying(50),
  usuario_modifica character varying(50)

=== seguridad.permiso ===
  id_permiso integer NOT NULL,
  id_rol integer,
  nombre_opcion character varying(100) NOT NULL

=== seguridad.rol ===
  id_rol integer NOT NULL,
  nombre_rol character varying(50) NOT NULL,
  descripcion text

=== seguridad.usuario ===
  id_usuario integer NOT NULL,
  username character varying(50) NOT NULL,
  password_hash character varying(255) NOT NULL,
  nombre_completo character varying(100),
  id_rol integer,
  activo boolean DEFAULT true

